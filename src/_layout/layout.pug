doctype HTML
html
  head
    meta(charset="utf8")
    -var add = data.tribute.home;
    title #{data.tribute.page.title}
    meta(name="description", content=data.tribute.page.description)
    meta(name="viewport", content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no")
      
    if config.entry.css.external
      link(rel="stylesheet", href="style.css")
  body
    .container
      block content
